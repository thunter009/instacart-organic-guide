- **[23:05] Shipped instacart-organic-guide v0.1.0 to AMO**: listed channel, status `nominated`. Listing description/tags/homepage set via API v5; screenshots still missing (0 previews).
- **[23:05] Live testing caught 2 wrong verdicts that lint + tests missed**: (1) Instacart renders PDPs as a modal over search results, so `document.querySelector('h1')` returned `Results for "strawberries"` — banner stamped BUY ORGANIC on organic strawberries. Fixed by reading the product name from the URL slug, not the DOM. (2) "Goat Cheese, Strawberry Spritz" matched as produce; added dairy/deli/confection terms. Rule: DOM-shape assumptions need a live check, stubs can't falsify them.
- **[23:05] Canned vs frozen resolved from sources, not intuition**: EWG ranks *fresh* produce (methodology.php); canning strips 90-100% of residues (PMC3907644) but freezing degrades ~nothing (thiram in frozen plums >80% after 49wk). So canned/jarred excluded, frozen kept. The naive "processed is processed" grouping was backwards.
- **[23:05] AMO submission gotchas**: 401 "Error decoding signature" = stray newline in the stored secret, not a bad key. Listed versions need license + categories via `--amo-metadata`. The v5 API escapes ALL HTML in `description` — plain text only.
